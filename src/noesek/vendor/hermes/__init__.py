"""Vendored Hermes Agent subsystems (NousResearch/hermes-agent, MIT).

Upstream source lives here verbatim except mechanical import-path rewrites into
this namespace; see each file's header and VENDORING.md at the repo root.
Bridge subpackages (hermes_cli/, agent/) are Noesek-authored adapters that let
the vendored code run inside Noesek's configuration and redaction stack.
"""
